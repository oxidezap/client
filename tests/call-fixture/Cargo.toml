[package]
name = "oxidezap-call-fixture"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
whatsapp-rust = { git = "https://github.com/oxidezap/whatsapp-rust", branch = "test/voip-call-fixture", default-features = false, features = ["test-support"] }
